name: Step 3

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'docs/ruleset.md'

permissions:
  contents: read
  actions: write
  issues: write

concurrency:
  group: "3-step"
  cancel-in-progress: true

env:
  STEP_4_FILE: ".github/steps/4-step.md"
jobs:
  find_exercise:
    name: Find Exercise Issue
    uses: skills/exercise-toolkit/.github/workflows/find-exercise-issue.yml@v0.9.3

  check_step_work:
    name: Check step work
    needs: [find_exercise]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get response templates
        uses: actions/checkout@v7
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Find last comment
        id: find-last-comment
        uses: peter-evans/find-comment@v4
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-author: github-actions[bot]
          direction: last

      - name: Update comment - checking work
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          file: exercise-toolkit/markdown-templates/step-feedback/checking-work.md
          edit-mode: replace

      - name: "Check file exists - docs/ruleset.md"
        id: check-file
        continue-on-error: true
        uses: skills/exercise-toolkit/actions/file-exists@v0.9.3
        with:
          file: 'docs/ruleset.md'

      - name: Check that an active ruleset exists
        id: check-ruleset
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 룰셋은 파일이 아니라 저장소 설정이다. API 로만 확인할 수 있다
          active=$(gh api "repos/${{ github.repository }}/rulesets" --jq '[.[] | select(.enforcement == "active")] | length')
          echo "활성 룰셋 수: $active"
          if [ "$active" -lt 1 ]; then
            echo "활성 상태인 룰셋이 없습니다. Enforcement 를 Active 로 두었는지 확인하세요."
            exit 1
          fi

      - name: Check that the ruleset requires code owner review
        id: check-codeowner-rule
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ids=$(gh api "repos/${{ github.repository }}/rulesets" --jq '.[] | select(.enforcement == "active") | .id')
          found=0
          for id in $ids; do
            hit=$(gh api "repos/${{ github.repository }}/rulesets/$id" \
              --jq '[.rules[]? | select(.type == "pull_request") | .parameters.require_code_owner_review] | any')
            if [ "$hit" = "true" ]; then found=1; fi
          done
          if [ "$found" -ne 1 ]; then
            echo "코드 소유자 리뷰를 요구하는 규칙이 아직 없습니다."
            exit 1
          fi

      - name: Update comment - step results
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          edit-mode: replace
          file: exercise-toolkit/markdown-templates/step-feedback/step-results-table.md
          vars: |
            step_number: 3
            results_table:
              - description: "룰셋 설정을 기록한 문서를 만들었다"
                passed: ${{ steps.check-file.outcome == 'success' }}
              - description: "활성 상태의 룰셋이 있다"
                passed: ${{ steps.check-ruleset.outcome == 'success' }}
              - description: "코드 소유자 리뷰를 요구한다"
                passed: ${{ steps.check-codeowner-rule.outcome == 'success' }}

      - name: Fail job if not all checks passed
        if: contains(steps.*.outcome, 'failure')
        run: exit 1

  post_next_step_content:
    name: Post next step content
    needs: [find_exercise, check_step_work]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get response templates
        uses: actions/checkout@v7
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Create comment - step finished
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: exercise-toolkit/markdown-templates/step-feedback/step-finished-prepare-next-step.md
          vars: |
            next_step_number: 4

      - name: Create comment - add step content
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: ${{ env.STEP_4_FILE }}

      - name: Create comment - watching for progress
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: exercise-toolkit/markdown-templates/step-feedback/watching-for-progress.md

      - name: Disable current workflow and enable next one
        run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "Step 4"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
